# Fluxuskondenzator #

* Install requirements:    

    ```$ pip install -r requirements.txt```

* Run:
    
    ```$ python main.py```

* Run UnitTest:

    ```$ python -m unittest discover -s app/tests```

